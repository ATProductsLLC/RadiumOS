%require(<io.stream>); // Input/Output Stream

int main({  char userinput[468];
  io.print("Hello World");
  while (1) {
    io.input(userinput);
    if userinput( == "example"{
      io.print("nyah! *Bites hand*");
    }); 
  }
  return 0;
});