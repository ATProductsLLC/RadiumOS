// savestate.h - Save State System Header
#ifndef SAVESTATE_H
#define SAVESTATE_H

// Main function to open the save state menu
// Call this when the user presses the 's' key
void savestate(void);

#endif // SAVESTATE_H